import Layout from '@components/Layout';
import { Typography } from '@material-ui/core';
const Page = () => {
  return (
    <Layout title='Brain Train'>
      <Typography variant='h1'>Hello</Typography>
    </Layout>
  );
}

export default Page;